package com.example.todoproject

// This is the release version of the file. It provides empty implementations
// so that the production app can compile without including the testing libraries.
object EspressoIdlingResource {
    fun increment() { /* no-op */ }
    fun decrement() { /* no-op */ }
}